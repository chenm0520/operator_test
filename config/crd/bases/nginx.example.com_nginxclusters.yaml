---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: nginxclusters.nginx.example.com
spec:
  group: nginx.example.com
  names:
    kind: NginxCluster
    listKind: NginxClusterList
    plural: nginxclusters
    singular: nginxcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: NginxCluster is the Schema for the nginxclusters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NginxClusterSpec defines the desired state of NginxCluster
            properties:
              image:
                default: nginx:latest
                description: Image is the nginx image to use
                type: string
              nginxConf:
                description: NginxConf is the nginx configuration content
                type: string
              replicas:
                default: 1
                description: Replicas is the number of nginx instances
                format: int32
                minimum: 1
                type: integer
            type: object
          status:
            description: NginxClusterStatus defines the observed state of NginxCluster
            properties:
              configHash:
                description: ConfigHash is the hash of current nginx config
                type: string
              lastUpdateTime:
                description: LastUpdateTime is the timestamp of last configuration
                  update
                format: date-time
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready replicas
                format: int32
                type: integer
              replicas:
                description: Replicas is the current number of replicas
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}


